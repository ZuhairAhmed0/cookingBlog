<div class="row flex-reverse align-items-center g-5 py-5 mb-4">
    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Huge selection of delicios recipe ideas</h1>
        <p class="lead">
            Explore our huge selection of delicious recipe ideas including; easy desserts, delicious vegan and
            vegetarian dinner ideas, gorgeous pasta recipes, quick bakes, family-friendly meals and gluten-free recipes.
        </p>

        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-dark btn-lg px-4 me-md-2">Explore latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>
    </div>
    <div class="col-12 col-lg-6">
        <img src="/img/hero-image.png" alt="Cooking With Node.js" width="607" height="510"
            class="d-block mx-lg-auto img-fluid " loading="lazy">
    </div>
</div>


<!-- Categpries Start -->

<div class="row row-cols-2 row-cols-lg-6 g-3 g-gl-3 py-4">

    <% if(categories != '') { %> 
        <% categories.forEach(({name, image}, index) => { %> 
            <a href="/categories/<%= name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="/img/<%= image %>" alt="<%= name %>" loading="lazy">
                </div>
                <div class="py-3">
                    <%= name %>
                </div>
            </a>
        <% })  %> 
    <% } %> 

    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="/img/view-all.jpg" alt="View All Categories" loading="lazy">
        </div>
        <div class="py-3">
            View All
        </div>
    </a>
    
</div>
<!-- Categpries End -->


<!-- Latest Start -->
<section class="pt-4 pb-4">
    <div class="d-flex align-items-center mb-2">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-3 g-gl-3">

        
        <% if(food != '') { %> 
            <% food.latest.forEach((recipe, index) => { %> 
                <a href="/recipe/<%= recipe._id  %>" class="col text-center category__link">
                    <div class="category__img category__img-large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
            <% })  %> 
        <% } %>

    </div>
</section>
<!-- Latest End -->


<!-- Thai Start -->
<section class="pt-4 pb-4">
    <div class="d-flex align-items-center mb-2">
        <h2>Thai Recipes</h2>
        <a href="/recipe/thai" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-3 g-gl-3">
        
        <% if(typeof food.thai !== "undefined" && food.thai.length  > 0)  { %> 
            <% food.thai.forEach((recipe, index) => { %> 
                <a href="/recipe/<%= recipe._id  %>" class="col text-center category__link">
                    <div class="category__img category__img-large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
            <% })  %> 
        <% } else { %>
            <p>No items found</p>
        <% } %> 

    </div>
</section>
<!-- Thai End -->

<!-- American Start -->
<section class="pt-4 pb-4">
    <div class="d-flex align-items-center mb-2">
        <h2>American Recipes</h2>
        <a href="/recipe/american" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-3 g-gl-3">
        
        <% if(typeof food.american !== "undefined" && food.american.length  > 0)  { %> 
            <% food.american.forEach((recipe, index) => { %> 
                <a href="/recipe/<%= recipe._id  %>" class="col text-center category__link">
                    <div class="category__img category__img-large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
            <% })  %> 
        <% } else { %>
            <p>No items found</p>
        <% } %> 

    </div>
</section>
<!-- American End -->

<!-- chinese Start -->
<section class="pt-4 pb-4">
    <div class="d-flex align-items-center mb-2">
        <h2>Chinese Recipes</h2>
        <a href="/recipe/chinese" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-3 g-gl-3">
        
        <% if(typeof food.chinese !== "undefined" && food.chinese.length  > 0)  { %> 
            <% food.chinese.forEach((recipe, index) => { %> 
                <a href="/recipe/<%= recipe._id  %>" class="col text-center category__link">
                    <div class="category__img category__img-large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
            <% })  %> 
        <% } else { %>
            <p>No items found</p>
        <% } %> 

    </div>
</section>
<!-- chinese End -->

<!-- Submit Start -->
<section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish-recipe.png" class="d-block mx-auto mb-2 img-fluid" alt="Publish Your recipe for FREE today" width="566" height="208">
    <h1 class="display-4 fw-bold">Publish Your recipe for FREE today</h1>
    <div class="dol-lg-6 mx-auto">
        <p class="load mb-4">
            Publish Your Recipe in front of thousands of people for free.
        </p>
        <div class="d-grid g-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-recipe" class="btn btn-primery btn-dark btn-lg"> Submit Recipe</a>
        </div>
    </div>
</section>
<!-- Submit End -->

